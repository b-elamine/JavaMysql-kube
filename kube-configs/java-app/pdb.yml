# When evicting pods, always keep at least 1 running at all times to prevent app down

apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: java-app-pdb
  namespace: pet-clinic-app
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: java-app
